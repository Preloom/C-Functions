void sortAlgorithm(int values[], int size)
{
  int i; 
  int j; 
  
  for(i = 0; i < size; i++)
  {
    for(j = 0; j < size; j++)
    {
      if(values[j] < values[i])
      {
        swapValues(&values[j], &values[i]);
      }
    }
  }
}
